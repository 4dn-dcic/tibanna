region: us-east-1

function_name: check_export_sbg 
handler: service.handler
role: lambda_full_s3
description: poll s3 until the export is finished 

# if access key and secret are left blank, boto will use the credentials
# defined in the [default] section of ~/.aws/credentials.
aws_access_key_id:
aws_secret_access_key:

# dist_directory: dist
# timeout: 15
# memory_size: 512
